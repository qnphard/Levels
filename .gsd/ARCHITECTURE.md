# Architecture

> Auto-generated by /map on 2026-01-18

## Overview

Levels is a React Native (Expo) mobile application focusing on meditation and perhaps gamified self-improvement ("Levels"). It features a rich UI with 3D elements (Skia/GL) and integrates with a backend AI service for Text-to-Speech (TTS) generation.

```mermaid
graph TD
    User[User] --> App[Mobile App (Expo)]
    subgraph Frontend
        App --> Screens[Screens]
        App --> L3D[Levels 3D Engine]
        Screens --> Services[ServicesLayer]
    end
    subgraph Backend
        Services -->|HTTPS| Modal[Modal.com TTS Service]
        Modal --> Py[Python Inference (MeloTTS)]
    end
```

## Components

### Frontend (React Native / Expo)
- **Location:** `src/`
- **Core:** `App.tsx`, `index.ts`
- **Screens:** `src/screens/` - UI pages (e.g., `RoomOfLevelsScreen`, `LevelDetailScreen`, `PhasesScreen`).
- **Components:** `src/components/` - Reusable UI elements (`GlassSurface`, `LivingBackground`).
- **3D Engine:** `src/levels3d/` - Custom 3D rendering logic (likely React Native Skia or Expo GL).
- **Services:** `src/services/` - Business logic and API clients.
    - `voiceTTSService.ts`: Client for the Modal TTS backend.
- **Theme:** `src/theme/` - Styling constants (Typography, Colors).

### Backend (Modal.com)
- **Location:** `modal_app.py`
- **Purpose:** Serverless Python environment for running AI models.
- **Function:** Text-to-Speech generation using MeloTTS.
- **Infrastructure:** Deployed to Modal.com, accessible via HTTPS endpoint.

## Data Flow

1.  **TTS Generation:**
    - User triggers an action requiring audio.
    - `voiceTTSService.ts` constructs a POST request to the Modal endpoint.
    - `modal_app.py` receives the text, generates audio using MeloTTS (on GPU/Cloud), and returns the audio (likely base64).
    - App decodes and plays the audio using `expo-av`.

2.  **Navigation:**
    - Managed by React Navigation (`@react-navigation/native`), likely using a Stack or Tab navigator structure within `App.tsx` (or similar).

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Modal.com | API (HTTPS) | Text-to-Speech (MeloTTS) hosting |
| Expo | Framework | Build, deployment, and native access |

## Technical Debt

- [ ] `src\screens\JournalScreen.tsx`: TODO - Save to persistent storage via context (Persistence missing).
- [ ] Hardcoded API URL in `voiceTTSService.ts` (Should be env var).

## Conventions

- **Naming:** PascalCase for Components/Screens (`LevelDetailScreen.tsx`), camelCase for services/vars.
- **Styling:** Tailwind CSS via `nativewind`.
- **Typing:** TypeScript used throughout (`.ts`, `.tsx`).
